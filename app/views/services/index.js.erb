var url = event.target.responseURL
var serviceId = url.substr(url.indexOf("=") + 1);
$('.task__service-type div').removeClass('active-type-service');
$('#' + serviceId + ' div').addClass('active-type-service');
$('#task-services').html("<%= j (render(partial: 'services', locals: { id: '' })) %>");
