<div id="wrapper">
  <div class="task-wrapper">
    <%= link_to "New task", new_task_path, remote: true, class: 'btn btn-demo new-task-btn' %>
  </div>

  <% @tasks.each do |task| %>
    <%= render partial: "task", locals: {task: task} %>
  <% end %>

  <div id="task-form"></div>
</div>
