$('#task-form').html("<%= j render(partial: 'form', locals: { btn_value: 'UPDATE TASK' }) %>");
$('#task-services').html("<%= j render(partial: 'services/services', locals: { id: @task.service.id }) %>");
$('#task-form').animate({ "right": "470px" }, "slow" );

var taskLatlng;

taskLatlng = {
  lat: parseFloat("<%= @task.latitude %>"),
  lng: parseFloat("<%= @task.longtitude %>")
};

reDrawMarkers(taskLatlng)
